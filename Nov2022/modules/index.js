// const amount = 12

// if (amount < 10)
//     console.log('small number')
// else
//     console.log('Big number');
// console.log('This is my first node code');

//  Modules

// simply modules means packages that are used in Java


// in global we have a function named require and here we are going to use that

const names = require('../modules/modules.js')


console.log(names);

// sayHi('Abhinav Jha')
// sayHi(john);
// sayHi(harry)

// Modules are simply encapuslated code (only share minimum)
// Node.js uses CommonJS , every file is module (by default)
// Every file in node.js is module